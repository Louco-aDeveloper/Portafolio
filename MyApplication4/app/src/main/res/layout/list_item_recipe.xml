<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="?android:attr/selectableItemBackground"
    android:layout_marginBottom="8dp">

    <!-- Nombre o título de la receta -->
    <TextView
        android:id="@+id/recipeName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/nombre_de_la_receta"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <!-- Descripción corta o resumen de la receta -->
    <TextView
        android:id="@+id/recipeSummary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/ingredientess"
        android:textSize="14sp"
        android:textColor="#666666"
        android:layout_marginBottom="8dp" />

    <!-- Tiempo de cocción o cualquier otro dato relevante -->
    <TextView
        android:id="@+id/recipeCookingTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tiempo_de_cocci_n_30_min"
        android:textSize="14sp"
        android:textColor="#666666" />

</LinearLayout>
